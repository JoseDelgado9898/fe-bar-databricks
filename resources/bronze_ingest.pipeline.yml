resources:
  pipelines:
    wealth_advisor_bronze:
      name: wealth_advisor_bronze
      catalog: ${var.catalog}
      schema: ${var.bronze_schema}
      serverless: true
      continuous: false
      configuration:
        landing_path: ${var.landing_path}
      libraries:
        - glob:
            include: ../src/**
